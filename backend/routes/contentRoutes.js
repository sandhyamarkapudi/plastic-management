const router=require('express').Router();const c=require('../controllers/contentController');const {authenticate,requireAdmin}=require('../middleware/auth');router.get('/',c.list);router.get('/:id',c.get);router.post('/',authenticate,requireAdmin,c.create);router.put('/:id',authenticate,requireAdmin,c.update);router.delete('/:id',authenticate,requireAdmin,c.remove);module.exports=router;
