document.addEventListener('DOMContentLoaded',()=>{const form=document.querySelector('#authForm');if(!form)return;form.addEventListener('submit',async e=>{e.preventDefault();const button=form.querySelector('button[type=submit]');button.disabled=true;try{const data=await api(form.dataset.mode==='register'?'/users/register':'/users/login',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(form)))});localStorage.setItem('token',data.token);localStorage.setItem('user',JSON.stringify(data.user));toast('Welcome, '+data.user.name+'!');setTimeout(()=>location.href='index.html',600)}catch(err){toast(err.message,false)}finally{button.disabled=false}})});
