document.addEventListener('DOMContentLoaded',async()=>{const target=document.querySelector('#dynamicContent');if(!target)return;try{const {data}=await api('/content');if(data.length)target.innerHTML=data.map(item=>`<article class="col-md-6 col-lg-4"><div class="card info-card"><div class="card-icon">♻</div><h3 class="h5">${item.title}</h3><p>${item.description}</p><span class="small-muted">${item.category}</span></div></article>`).join('')}catch(e){/* Static page content remains useful without the API. */}});
