const router=require('express').Router();const c=require('../controllers/quizController');const {authenticate,requireAdmin}=require('../middleware/auth');router.get('/',c.list);router.post('/submit',c.submit);router.post('/questions',authenticate,requireAdmin,c.create);router.put('/questions/:id',authenticate,requireAdmin,c.update);router.delete('/questions/:id',authenticate,requireAdmin,c.remove);module.exports=router;
